server {
    listen 3000;
    root /usr/share/nginx/html;

    location / {
        if (-d $request_filename) {
            rewrite ^ /index.html break;
        }
        try_files $uri /index.html;
    }
}
