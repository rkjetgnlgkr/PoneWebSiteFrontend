server {
    listen 3000;
    root /usr/share/nginx/html;

    location = /login {
        try_files /index.html =404;
    }

    location = /portfolio {
        try_files /index.html =404;
    }

    location / {
        try_files $uri /index.html;
    }
}
